import React from "react";

const TypeRow = ({ sort }) => (
  <div className="standingsTable typeRow">
    <span className="Type-clip">Clips</span>
    <span className="Type-home">At home</span>
    <span className="Type-away">Away</span>
  </div>
);

export default TypeRow;
